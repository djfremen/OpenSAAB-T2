# Existing deployment preserved; no credentials or database copied from this checkout.
FROM djfremen/saab-security-api:t2-arm32-landscape-2026-09-15
COPY support_reports_api.py support_entrypoint.py /app/
CMD ["sh", "-c", "uvicorn support_entrypoint:app --host 0.0.0.0 --port ${PORT:-8000}"]
